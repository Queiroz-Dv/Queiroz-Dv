
name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.d2f372c8-04bf-4271-8ff8-e4dc5ed48274 }}
          GH_TOKEN: ${{ secrets.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 }}
